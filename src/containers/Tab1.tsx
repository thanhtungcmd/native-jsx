import { observer } from 'mobx-react';
import React from 'react';
import { Text, View } from 'react-native';

const Tab1 = observer(() => {

    return (
        <View>
            <Text>
            Tab1
            </Text>
        </View>
    );
});

export default Tab1;