import { observer } from 'mobx-react';
import React from 'react';
import { Text, View } from 'react-native';

const Tab2 = observer(() => {

    return (
        <View>
            <Text>
            Tab2
            </Text>
        </View>
    );
});

export default Tab2;