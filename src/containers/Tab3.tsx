import { observer } from 'mobx-react';
import React from 'react';
import { Text, View } from 'react-native';

const Tab3 = observer(() => {

    return (
        <View>
            <Text>
            Tab3
            </Text>
        </View>
    );
});

export default Tab3;